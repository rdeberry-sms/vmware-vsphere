network:
  version: 2
  ethernets:
    ens192:
      dhcp4: false
      addresses:
        - ${ip_address}/${netmask}
      gateway4: ${gateway}
      nameservers:
        addresses: [${dns_server}]
local-hostname: ${hostname}
instance-id: ${hostname}
